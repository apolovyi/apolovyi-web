if [ -x "$HOME/.claude/git-hooks/pre-commit" ]; then
  "$HOME/.claude/git-hooks/pre-commit" "$@" || exit 1
fi

pnpm exec lint-staged && node scripts/generate-claude-docs.mjs && git add -f .claude/docs/
