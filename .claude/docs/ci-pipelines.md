# CI Pipelines Reference

> Auto-generated by `scripts/generate-claude-docs.mjs` — do not edit manually.

## Workflows

### CI (`ci.yml`)

- **Triggers**: push, pull_request
- **Steps**: Validate dictionaries → Prettier check → Lint (CI strict) → Build
- **Config**: `.github/workflows/ci.yml`

### Claude Code Review (`claude-code-review.yml`)

- **Triggers**: pull_request
- **Steps**: Checkout repository → Run Claude Code Review
- **Config**: `.github/workflows/claude-code-review.yml`

### Claude Code (`claude.yml`)

- **Triggers**: issue_comment, pull_request_review_comment, issues, pull_request_review
- **Steps**: Checkout repository → Run Claude Code
- **Config**: `.github/workflows/claude.yml`

### E2E Tests (`e2e.yml`)

- **Triggers**: pull_request, workflow_dispatch
- **Steps**: Build application → Determine test scope → Run E2E tests (${{ steps.test-scope.outputs.scope }}) → Upload test results
- **Config**: `.github/workflows/e2e.yml`

### Smoke Test (`smoke.yml`)

- **Triggers**: push, pull_request
- **Steps**: Run smoke tests
- **Config**: `.github/workflows/smoke.yml`
